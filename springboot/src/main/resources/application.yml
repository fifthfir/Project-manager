server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/proj_manager?serverTimezone=GMT%2d8
    username: root
    password: 123456
mybatis:
    mapper-locations: classpath:mapper/*.xml     # Scan all .xml file
#    configuration:
#      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

files:
  upload:
    path: G:\proj-manager\files\

